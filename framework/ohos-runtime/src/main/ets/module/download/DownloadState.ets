export enum DownloadState {
  DOWNLOAD_STATE_INIT = 0,
  DOWNLOAD_STATE_START = 1,
  DOWNLOAD_STATE_PROGRESS = 2,
  DOWNLOAD_STATE_STOP = 3,
  DOWNLOAD_STATE_CANCEL = 4,
  DOWNLOAD_STATE_SUCCESS = 5,
  DOWNLOAD_STATE_ERROR = 6,
}
